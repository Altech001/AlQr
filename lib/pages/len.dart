import 'package:flutter/material.dart';

class LenPage extends StatelessWidget {
  const LenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}